# DemoGuard

DemoGuard is a Java Kubernetes operator intended to help hackathon teams prepare zero-downtime Kubernetes demos.

This repository currently contains only the initial project skeleton. The operator module uses Java 21, Maven, Java Operator SDK, Fabric8 Kubernetes Client, and JUnit 5.

## Build

```bash
cd operator
mvn test
```

